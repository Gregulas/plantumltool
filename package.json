{
  "name": "plantuml-local-studio",
  "version": "3.3.1",
  "private": true,
  "type": "module",
  "description": "Offline local PlantUML editor and renderer with type-based syntax highlighting, inline color picker, collapsible blocks, autocomplete, formatting, diagnostics, semantic reference warnings, resizable panels, last-known-good rendering, and diagram-to-source navigation.",
  "scripts": {
    "postinstall": "node scripts/patch-plantuml-browser-limit.mjs",
    "predev": "node scripts/patch-plantuml-browser-limit.mjs",
    "dev": "vite --force --host 127.0.0.1",
    "prebuild": "node scripts/patch-plantuml-browser-limit.mjs",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1",
    "test": "node --test tests/*.test.mjs"
  },
  "dependencies": {
    "@plantuml/core": "1.2026.6",
    "dictionary-en": "^4.0.0",
    "nspell": "^2.1.5"
  },
  "devDependencies": {
    "vite": "^7.0.0"
  }
}
